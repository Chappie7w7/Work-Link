{% extends "components/base.jinja2" %}
{% import "components/footer.jinja2" as footer %}

{% block title %}Recuperar Contraseña{% endblock %}

{% block content %}
<div class="container">
    <div class="login-box">
        <h2>Recuperar Contraseña</h2>
        
        {% with messages = get_flashed_messages(with_categories=true) %}
            {% if messages %}
                {% for category, message in messages %}
                    <div class="alert alert-{{ category }}">{{ message }}</div>
                {% endfor %}
            {% endif %}
        {% endwith %}
        
        <form method="post" action="{{ url_for('LoginRoute.recuperar') }}">
            <div class="form-group">
                <label for="correo">Ingresa tu correo electrónico:</label>
                <input type="email" id="correo" name="correo" required>
            </div>
            
            <button type="submit" class="btn-entrar">Enviar enlace</button>
        </form>
        
        <div class="form-footer">
            <a href="{{ url_for('LoginRoute.login_form') }}" class="btn-as-text">Volver al inicio de sesión</a>
        </div>
    </div>
</div>
{% endblock %}

{{ footer.footer() }}