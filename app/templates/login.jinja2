<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Formulario de Login</title>

    <!-- Fuentes y estilos externos -->
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400;500;600;700&display=swap"
        rel="stylesheet" />
    <link href="https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css" rel="stylesheet" />

    <!-- Tu archivo CSS -->
    <link rel="stylesheet" href="{{ url_for('static', filename='css/style_login.css') }}" />
</head>

<body>
    <!-- Formulario de Login -->
    <div class="container-form login" id="loginForm">
        <button class="btn-back" onclick="window.location.href='{{ url_for('IndexRoute.index') }}'">
            ←
        </button>
        <div class="information">
            <div class="info-childs">
                <h2>Bienvenido nuevamente</h2>
                <p>Para unirte a nuestra comunidad, por favor inicia sesión con tus datos</p>

                <!-- Dropdown con las opciones "Soy usuario" y "Soy empresa" -->
                <div class="dropdown">
                    <button id="sign-up">
                        Registrarse <span class="arrow">▼</span>
                    </button>
                    <div class="dropdown-content">
                        <a href="{{ url_for('LoginRoute.form_usuario') }}" id="usuario">Soy usuario</a>
                        <a href="{{ url_for('LoginRoute.form_empresa') }}" id="empresa">Soy empresa</a>
                    </div>
                </div>

            </div>
        </div>

        <!-- Caja del formulario de login -->
        <div class="form-information">
            <div class="form-information-childs">
                <h2>Iniciar sesión</h2>

                <!-- Mostrar mensajes flash -->
                <div class="mensajes">
                    {% with mensajes = get_flashed_messages(with_categories=true) %}
                    {% if mensajes %}
                    {% for categoria, mensaje in mensajes %}
                    <p style="color: {% if categoria == 'error' %}red{% else %}green{% endif %};">
                        {{ mensaje }}
                    </p>
                    {% endfor %}
                    {% endif %}
                    {% endwith %}
                </div>

                <!-- Formulario de login -->
                <form method="post" action="{{ url_for('LoginRoute.login') }}" class="form">
                    <div>
                        <label>
                            <i class='bx bx-envelope'></i>
                            <input type="email" placeholder="Correo Electrónico" name="correo" required />
                        </label>
                    </div>
                    <div>
                        <label>
                            <i class='bx bx-lock-alt'></i>
                            <input type="password" placeholder="Contraseña" name="password" required />
                        </label>
                    </div>
                    <input type="submit" value="Iniciar sesión" />
                </form>

                <!-- Enlace para "Olvidaste tu contraseña" -->
                <form method="get" action="">
                    <a href="{{ url_for('LoginRoute.recuperar') }}" class="link-recuperar">¿Olvidaste tu contraseña?</a>
                </form>

            </div>
        </div>
    </div>

    <!-- Tu archivo JS -->
    <!--script src="{{ url_for('static', filename='js/login.js') }}"></script-->
</body>

</html>